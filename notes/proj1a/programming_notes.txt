Apache log location: ?
Execute perl from command line:
		perl -e 'command here'
		
Back up to remote server script:

apache2 access and error log location: /var/log/apache2
empty file: cat /dev/null > filename1

unlock keyboard mode: ctrl+alt+win




for sid in onlydirs:
	submission = submission_dir + sid
	resultsFile.write(submission + ":\t")
	
	#ret_value = os.system('mysql CS144 < ' + submission_dir + '003116583/create.sql')
	ret_value = os.system('mysql CS143 < ' + submission + '/create.sql')
	signal_number = (ret_value & 0x0F)
	if not signal_number:
		exit_status = (ret_value >> 8)
		if exit_status:
			print "ERROR: problem with command"
			resultsFile.write(" ERROR: problem with command\n");
		else:
			resultsFile.write(" Loaded Successfully\n");
		
resultsFile.close()
