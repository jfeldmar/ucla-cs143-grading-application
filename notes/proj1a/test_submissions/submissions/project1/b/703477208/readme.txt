Tarun
Solanki
703-477-208
the project takes the queries and runs it. it opens a connection to database CS143 with username cs143.
it runs the given query, and then from the result, obtains the number of rows and columns and runs a for loop
that displays them one by one. the html tags are genereated use echo statments. the method type is get and the actions are handled by 
query.php.
